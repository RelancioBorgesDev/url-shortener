global:
  scrape_interval: 10s
  evaluation_interval: 10s

remote_write:
  - url: http://mimir:9009/prometheus/api/v1/push

scrape_configs:
  - job_name: url-shortener-api
    metrics_path: /metrics
    static_configs:
      - targets: ["url-shortener:3333"]
