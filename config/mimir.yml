# Mimir single-node config
multitenancy_enabled: false

blocks_storage:
  backend: filesystem
  filesystem:
    dir: /tmp/mimir/data/blocks

server:
  http_listen_port: 9009
  grpc_listen_port: 9008

ingester:
  ring:
    replication_factor: 1

usage_stats:
  enabled: false
